apply plugin: 'com.android.library'

android {
    namespace 'com.yuanzhou.vlc'
    // Follow the app rather than pinning separately, so a toolchain bump moves one file.
    compileSdk rootProject.ext.compileSdkVersion

    defaultConfig {
        minSdkVersion rootProject.ext.minSdkVersion
        targetSdkVersion rootProject.ext.targetSdkVersion
        consumerProguardFiles("proguard-rules.pro")
        ndk {
            // Match what the app actually ships. Building x86 and x86_64 here cost build
            // time on every native compile and shipped in nothing.
            abiFilters(*rootProject.ext.nativeArchitectures)
        }
        externalNativeBuild {
            cmake {
                cppFlags ""
            }
        }
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
    externalNativeBuild {
        cmake {
            path file('src/main/cpp/CMakeLists.txt')
        }
    }
}

repositories {
    google()
    mavenCentral()
}

dependencies {
    // The PiP controller needs ComponentActivity.addOnPictureInPictureModeChangedListener,
    // which landed in androidx.activity 1.8.0. React Native pins the app to the same
    // version, so this does not change what ships.
    implementation 'androidx.activity:activity:1.8.0'
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    // No version: React Native's Gradle plugin contributes a strict constraint, so the
    // module always matches the RN the app is built against.
    implementation 'com.facebook.react:react-android'
    implementation 'org.videolan.android:libvlc-all:3.6.5'
    // Media3 owns the media session, its notification, and media button handling.
    // Media3 1.10.0 fixed stale foreground-service intents that could crash Android with
    // ForegroundServiceDidNotStartInTimeException. The new toolchain supports 1.11.0.
    implementation 'androidx.media3:media3-session:1.11.0'
    implementation 'androidx.media3:media3-common:1.11.0'
}

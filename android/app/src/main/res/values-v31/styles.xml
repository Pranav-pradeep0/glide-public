<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Splash theme for Android 12+ (API 31+) -->
    <!-- This allows us to override the default "Icon in Circle" look -->
    <style name="SplashTheme" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo_animated</item>
        <item name="android:windowSplashScreenAnimationDuration">800</item>
        <item name="postSplashScreenTheme">@style/AppTheme</item>
    </style>
</resources>
